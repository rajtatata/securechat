import React from 'react'
import { Ionicons } from '@expo/vector-icons'

import colors from '../../../assets/predefinedColors/predefinedColors'

const backButton = (
    <Ionicons name="md-arrow-back" size={25} color={colors.black} />
)

export default backButton